# Express with Decorators, Metadata and Classes

Wiring up together some code which uses:

- Typescript
- Express
- Metadata
- Decorators

## Quickstart

- `$ npm install`
- `$ npm start:build`
- `$ npm start:run`

Visit your browser at `http://localhost:3000`
